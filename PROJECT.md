# GM_League — 製作方針レポート

> 進行管理用ドキュメント。「何を作るか」の確定定義は SPEC.md を参照。
> こちらは方針・原則・進捗を追うためのもの。作業のたびにここに戻れば現在地が分かる。
> 最終更新：2026-08-17 / 現在地：Phase 0 完了、Phase 1 着手

---

## 1. このプロジェクトの目的（1行）

J リーグ選手限定の eFootball 私設大会を、独自通貨・移籍・特別ルール込みで運営できるツールを作る。

---

## 2. 技術スタック

| 層 | 採用 | 役割 |
|---|---|---|
| フロント | GitHub Pages（HTML/CSS/Vanilla JS） | 画面・操作 |
| 認証 | Google Identity Services | ログイン、IDトークン発行 |
| バックエンド | GAS Web App（GASエディタで直接編集・デプロイ） | APIゲートウェイ。全書き込みの検証・承認・時刻判定 |
| DB | Google Sheets | データ本体。1シート=1テーブル |
| 補助入力 | Google Form | スポンサー収益の入力のみ |

### 確定値
- プロジェクト名：GM_League
- GitHubリポジトリ：hinata082121-gif/GM_League
- 公開URL：https://hinata082121-gif.github.io/GM_League/
- OAuthクライアントID：1078761144028-uor0b4ukacklepi7g6u4i6jn90q33qc0.apps.googleusercontent.com
- spreadsheetId：1pi8-gYlKfc_fe_F4iY1idp3fD6lJMzMhW2HbLdQ42aM
- OAuth生成元：https://hinata082121-gif.github.io / http://localhost:8000

---

## 3. 絶対に守る設計原則（5つ）

この5つを外すと後で必ず破綻する。実装中に迷ったらここに戻る。

1. 書き込みは必ず GAS 経由。クライアントから Sheets 直書きしない。読み取りのみ直叩き可。
2. 時刻判定はサーバー（GAS）側。プロテクト期限・割引時間帯はクライアント時計を信用しない。
3. 予算残高はカラムで持たない。常に BudgetTx の合計（SUM）で算出。
4. 移籍は「買い手支払」と「売り手受取」を別カラム。
5. 承認前データを集計に混ぜない。順位表・ランキングは status=承認 のみで都度導出。

補助原則：金額・人数・率・時刻はすべて Config シート参照（直書き禁止）。同時申請対策に GAS の LockService で書き込みを直列化。

---

## 4. 開発の進め方

- フェーズ単位で進める。各フェーズは単体で動作確認できる区切り。前フェーズが動いてから次へ。
- 読み取りから書き込みへ。先に閲覧系（低リスク）を固め、移籍・集計（高リスク）は後半。
- 仮値で進める。賞金・スポンサー額が未定でも Config の仮値で実装可能。確定後に値を差し替えるだけ。
- 各フェーズ完了時にこのレポートの進捗表を更新する。

---

## 5. フェーズ別ロードマップと進捗

| Phase | 内容 | 状態 | メモ |
|---|---|---|---|
| 0 | 基盤：Pages雛形 + Googleログイン + GASスケルトン + Sheetsセットアップ | ✅ 完了 | GASエディタ直編集運用（clasp不採用） |
| 1 | マスタ & 閲覧：Players/Teams/Users 登録、他チーム閲覧、予算表示 | ✅ 完了 | Clubsマスタ・2段プルダウン追加 |
| 2 | エントリー：提出（新規28/継続引継ぎ）、22–35名検証 | ✅ 完了 | 承認制。setSeasonStatus を前倒し実装 |
| 3 | 移籍：申請（2段プルダウン）、特別/無効化/オークション、承認→BudgetTx計上 | ✅ 完了 | 売り手承認を挟む3段階フロー。予算は申請時に予約 |
| 4 | プロテクト：無料2/有料3、サーバー時刻の期限ゲート、掲示 | ✅ 完了 | 解除不可・枠は戻らない・獲得選手も対象 |
| 5 | 試合集計：申請（スコア/シュート/得点/アシスト/GK）、承認/差戻/訂正 | ✅ 完了 | 得点者行をスコアに連動。オウンゴールは番兵値 |
| 6 | 集計表示：順位表、トーナメント（1stレグ+合計+PK）、各ランキング | ⬜ 未着手 | |
| 7 | 経済周辺 & 進行：罰金/補填/スポンサー、advanceSeason/closeSeason | ⬜ 未着手 | |
| 8 | 仕上げ：過去大会記録、UI調整、エッジケース | ⬜ 未着手 | |

> 状態の凡例：⬜ 未着手 / 🔄 作業中 / ✅ 完了

---

## 6. リスク・要注意ポイント

| 項目 | リスク | 対応 |
|---|---|---|
| 同時申請 | 移籍・試合申請が同時に走るとデータ競合 | GAS LockService で直列化 |
| 時刻詐称 | プロテクト期限・割引をクライアントで判定すると抜け道 | GAS 側で new Date() 判定 |
| 予算の二重管理 | 残高カラムと取引履歴がズレる | 残高は持たず SUM 算出に統一 |
| 承認前の混入 | 申請中データが順位に反映される | 集計は status=承認 のみ |
| 現実移籍の反映漏れ | eligible 更新を忘れると不正エントリー | 主催者運用＋エントリー時チェック |
| Sheets行数肥大 | 履歴が増えて読み取りが重くなる | season_id で絞って読む。必要なら季別シート分割を検討 |

---

## 7. 未確定事項（実装に影響しないが、いずれ確定が必要）

- 賞金額（シーズン賞金・順位別賞金・得点王賞金）→ Config 仮値（0）で進行中。
- スポンサー額 → 毎シーズン第1次市場前にフォーム入力。
- トーナメントで 2ndレグ制を毎回使うか単発もあるか → Seasons.leg_enabled で切替可。運用判断でOK。

---

## 8. 関連ドキュメント

- SPEC.md … 確定仕様（データモデル・経済ルール・API・画面）
- PROJECT.md … 本ファイル（方針・進捗）
- CLAUDE.md … Claude Code 用。上記2つを参照させる
