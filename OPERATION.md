# 運用マニュアル（主催者向け）

大会を実際に回すための手順書。技術的な仕様は `SPEC.md`、開発の経緯は `PROJECT.md` を参照。

---

## 1. 本番開始までの準備

**順序に意味がある。** 選手 → チーム → ユーザー → シーズンの順に登録する。
先にチームを作らないとユーザーに所属チームを割り当てられず、
選手がいないとエントリーができない。

### 1-1. テストデータを消す

GAS エディタで **`clearTestData`** を実行する。
`seed_` で始まる行と、テスト用シーズンに紐づく行がすべて消える。

> 動作確認用に作ったシーズン（E2E検証シーズンなど）が残っている場合は、
> Seasons シートから手動で行を削除する。紐づく Rosters / Matches / BudgetTx も
> 同じ `season_id` で絞って消しておく。

### 1-2. 選手マスタを投入する

「マスタ管理」タブの **選手 CSV 一括登録** を使う。

```
name,position,real_club
三笘薫,MF,川崎フロンターレ
権田修一,GK,清水エスパルス
```

- 1行目はヘッダー。`name` と `position` が必須、`real_club` は任意
- `position` は `GK` / `DF` / `MF` / `FW` のいずれか
- 同名かつ同ポジションの選手は自動でスキップされるので、追加投入も安全
- 数百行を一度に貼っても問題ない

クラブ名は `Clubs` シートの表記に揃えると、後で選手登録画面のプルダウンと一致する。

### 1-3. チームを登録する

「マスタ管理」タブの **チームマスタ** から1チームずつ登録する。
初年度は全チーム `新規`（エントリーで28名を選ぶ）。

### 1-4. ユーザーを登録する

「マスタ管理」タブの **ユーザーマスタ** から、参加者の Gmail を登録する。

| 項目 | 内容 |
|---|---|
| メールアドレス | 参加者の Google アカウント |
| 表示名 | 画面に出る名前 |
| ロール | `チームオーナー` |
| 所属チーム | 1-3 で作ったチーム |

**あわせて OAuth の設定も必要。** Google Cloud Console → OAuth 同意画面 →
テストユーザーに参加者の Gmail を追加する。ここを忘れると、
アプリに登録してあってもログイン自体ができない。

### 1-5. シーズンを作る

「運営・進行」タブでは既存シーズンの操作しかできないため、
最初の1シーズンだけは Seasons シートに直接1行足すか、
2シーズン目以降と同じく `upsertSeason`（アプリからの作成）を使う。

| カラム | 例 |
|---|---|
| season_id | `s2026_1` |
| name | `2026シーズン` |
| status | `準備中` |
| leg_enabled | `TRUE`（トーナメントを2レグ制にするなら） |
| claim_deadline_at | `2026/12/20 23:59`（補填の選択期限。翌日に精算する） |
| window1_open_at | `2026/09/01 20:00` |
| window2_open_at | `2026/11/01 20:00` |

**移籍市場の開幕日時はプロテクトの期限計算に直結する。**
無料プロテクトは開幕の前々日いっぱい、有料は前日23時から。
日時を後から変えると期限も動くので、参加者に告知した後の変更は避ける。

### 1-6. Config を確認する

`Config` シートで金額と人数を最終確認する。賞金は大会別に分かれている。

**リーグ（参加チーム数で金額が変わる）**

| key | 一部制 | key | 二部制 GM1 | key | 二部制 GM2 |
|---|---|---|---|---|---|
| `prize_gm1_1div_1` | 1位 | `prize_gm1_2div_1` | 1位 | `prize_gm2_1` | 1位 |
| `prize_gm1_1div_2` | 2位 | `prize_gm1_2div_2` | 2位 | `prize_gm2_2` | 2位 |
| `prize_gm1_1div_3` | 3位 | `prize_gm1_2div_3` | 3位 | `prize_gm2_3` | 3位 |
| `prize_gm1_1div_4` | 4位 | `prize_gm1_2div_4` | 4位 | | |
| | | `prize_gm1_2div_5` | 5位 | | |

**カップ戦・その他**

| key | 説明 |
|---|---|
| `prize_cup_1` / `prize_cup_2` / `prize_cup_semi` | GMリーグ杯の優勝 / 準優勝 / ベスト4 |
| `prize_supercup_1` / `prize_supercup_2` | GMスーパーカップの優勝 / 準優勝 |
| `supercup_stream_fee` | スーパーカップの配信料（出場2チームそれぞれに満額） |
| `top_scorer_gm1` / `top_scorer_gm2` / `top_scorer_cup` | 大会別の得点王賞金 |
| `two_division_min_teams` | 二部制にできる最低チーム数（既定 15） |

金額を変えるとその後の計算に即反映される。既に計上済みの取引は変わらない。

> `season_prize` / `rank_prize_1〜3` / `top_scorer_prize` は旧体系の名残で、
> 現在はどこからも参照していない。0 のままにしておく。

### 1-6.5. 参加者を募集する

「参加登録承認」タブで **合言葉を決めて、受付をONにする**。

1. 合言葉を入力して「保存する」（大文字小文字と空白は区別されない）
2. 「参加登録を受け付ける」にチェックを入れて保存
3. 同じ画面に出ている**参加登録URL**を告知に使う

**URLと合言葉は別々に伝える。** URLだけでは登録できない作りなので、
URL は X などで公開してよく、合言葉だけを参加を認めた人に渡せばよい。

申請が来たら同じタブの「申請一覧」に並ぶ。**承認するとユーザーとチームが同時に作られ、
その人がログインできるようになる。** 承認するまでは大会のデータに一切影響しない。

**参加者はチーム名を自由に入力できない。** J1・J2 の実在クラブから選ぶ方式で、
既に使われているクラブは選択肢に出ても選べない（「登録済み」「申請中」と表示される）。

J3 を解禁したくなったら、Config の `signup_club_categories` を `J1,J2,J3` にする。
Clubs シートには J3 の20クラブが最初から入っているので、シートを触る必要はない。

| 状況 | 対応 |
|---|---|
| 別のクラブに変えたい | 承認前に一覧のプルダウンで選び直してから承認する |
| 希望クラブが埋まっている | 承認が拒否される。空いているクラブに変えてから承認する |
| 定員に達した | 「受け付ける」のチェックを外す。合言葉を変えるのも有効 |
| 間違って却下した | 本人に再申請してもらう（同じ合言葉で出し直せる） |

募集を締め切ったら、**受付のチェックを外す**。合言葉を空にしても受付は止まる。

### 1-6.7. 日程表を作る

「運営・進行」タブの **日程表** で、**リーグ戦の開幕日**を入れて「日程を作成」を押す。
ひな型から逆算して、準備期間の日付が全部決まる。

**毎シーズン同じ並びを使い回す。** 変えるのは開幕日だけでよい。

| やりたいこと | 操作 |
|---|---|
| 1つの予定だけ日付をずらす | 一覧の「編集」。他の日程は動かない |
| 予定を足す・消す | 「予定を追加」／一覧の「削除」 |
| 消化済みにする | 「編集」で done にチェック。次の予定から外れる |
| **毎シーズンの並び自体を変える** | 「日程表のひな型を編集する」を開いて保存 |

ひな型の日数は**開幕日を0とした相対日数**。開幕14日前なら `-14`、開幕当日なら `0`。
同じ日数の行は上から順に並ぶ。

既定のひな型は前シーズン（6/1開幕）の日程から逆算した実績値。
**「継続参加者の募集期限」の日数だけ仮**（-19）なので、実際の運用に合わせて直すこと。

### ツールの外で行う予定

日程表には載るが、**ツールに機能が無い、または自動では動かない**ものがある。

| 予定 | ツールの動き | 主催者がやること |
|---|---|---|
| 新規募集終了 | 日付が来ても受付は開いたまま | 「参加登録承認」で受付のチェックを外す |
| 継続参加者の募集期限 | 継続の意思を記録する場所が無い | X で確認し、辞退者は「辞退・チーム変更」で処理する |
| 使用監督申告（第一次・第二次） | **機能なし** | X とスプレッドシートで受け付ける |
| 使用監督の抽選 | **機能なし** | 重複したら主催者が抽選する |
| 各種の掲示・発表 | 掲示する場所は用意していない | X などで告知する |

**新規の募集は X で行い、登録リンクは応募者へ個別に送る。**
登録ページの URL は公開してもよいが、合言葉が無いと申請できない。
募集を締め切ったら受付のチェックを外す（合言葉を変えるのも有効）。

継続参加はシーズン終了処理で**自動的にスカッドが引き継がれる**仕組みなので、
「続けます」という申告そのものは不要。**続けない人を辞退として処理する**だけでよい。
返答が無い人をどう扱うかは運用で決める（既定では何もしなければ継続扱いになる）。

**ひな型を直しても、作成済みの日程は変わらない。** 次に作成する分から反映される。
今シーズンにも反映したい場合は「上書き」にチェックして作り直す
（個別に調整した内容は消える）。

作成した日程は参加者の「日程」タブと公開ページにも出る。
今日の行が強調され、次の予定までの残り日数が表示される。

### 1-7. ディビジョンを割り当てる

「運営・進行」タブの **ディビジョン設定** で、各チームを GM1 / GM2 に振り分ける。

- 参加チームが `two_division_min_teams`（既定15）に達していないシーズンは、
  GM2 を選んでも保存時に拒否される。この場合は全チーム GM1（一部制）のまま進める。
- 一部制と二部制では**同じ1位でも賞金額が違う**。割り当てを変えると
  シーズン終了時に支給される額も変わる。

### 1-8. GMスーパーカップを設定する

「運営・進行」タブの **GMスーパーカップ** で出場2チームを指定する。
前シーズンの GM1リーグ王者と GMリーグ杯王者が候補として表示されるが、
**自動では入力されない**（同一チームが両方の王者になる場合があるため）。

他サイト（ミラティブ・TikTokライブなど）で配信を行ったら
**「他サイトで配信を行った」にチェック**を入れる。
配信料は**試合の勝敗と無関係に**出場2チームへ支給される。

### 1-9. テストデータを消す（本番開始前に1回だけ）

動作確認で作ったチーム・シーズン・試合を消して、まっさらな状態から始める。

1. GASエディタで **`previewReset`** を実行する。**何も消えない。** 実行ログに
   「何がいくつ消えるか」と「残る主催者アカウント」が出るので、内容を確認する
2. 問題なければ `seed.gs` の先頭付近にある `RESET_CONFIRMED` を **`true`** に変える
3. **`resetAllTournamentData`** を実行する
4. 終わったら `RESET_CONFIRMED` を **`false`** に戻す

| 消えるもの | 残るもの |
|---|---|
| シーズン・チーム・スカッド・エントリー | 選手マスタ（Players） |
| 移籍・プロテクト・予算取引 | クラブ一覧（Clubs） |
| 試合と付随データ・ディビジョン・スーパーカップ | Config の設定値すべて |
| 参加申請（Signups）・role=team のユーザー | 主催者アカウント |

**取り消せない。** フラグを二段構えにしているのは、GASエディタの実行プルダウンに
全関数が並ぶため、後から誤ってクリックする事故を防ぐため。

主催者が1人もいない状態では自動で中止する（実行しても誰もログインできなくなるだけのため）。

---

## 2. シーズンの回し方

「運営・進行」タブの **次の状態へ進める** を押すと1段階ずつ進む。
状態ごとに主催者がやることは以下のとおり。

| 状態 | 主催者がやること |
|---|---|
| 準備中 | 上記1の準備。ディビジョン割り当てもここで済ませる |
| **エントリー受付** | スポンサー収益を入力する。各チームの提出を「エントリー承認」で承認する |
| **移籍市場1** | 移籍申請を「移籍承認」で承認/差戻。オークション結果を登録する |
| シーズン1 | 試合結果を「試合」タブで承認する |
| **移籍市場2** | 進めた瞬間に半期期限付きの選手が離脱する。以降は市場1と同じ |
| シーズン2 | 試合結果の承認 |
| トーナメント | GMリーグ杯・GMスーパーカップの承認。2レグ制なら `tie_id` と `レグ` を揃える |
| 終了 | **`closeSeason`** を実行（次項）。**賞金はすべてここで支払われる** |

> **賞金はシーズン中には一切支払われない。** 順位賞金もカップ賞金も配信料も、
> すべてシーズン終了処理でまとめて計上する。
> これは終了手数料（残予算の10%）の母数を全チームで揃えるため。

### プロテクトの受付時期

シーズン状態とは独立して、**移籍市場の開幕日時から逆算**して自動で切り替わる。

```
      無料期（枠2・0円）        空白        有料期（枠3・有料）
──────────────────────┼──────────┼─────────────────────
   〜 前々日 23:59:59    前日0:00〜22:59   前日23:00 〜 市場最終日23:59
```

参加者から「プロテクトできない」と言われたら、まず今がどの期間かを確認する。
「プロテクト」タブに現在のフェーズと受付期間が表示される。

### 現実移籍の反映

Jリーグの選手が**大会に参加していないクラブへ移籍した**場合、その選手は翌シーズンから使えない。

「運営・進行」タブの **現実移籍の反映** で、選手名か実クラブ名で検索し、
対象にチェックを入れて「選択した選手を対象外にする」を押す。

押す前に、以下が画面に出る。

- 補填金の合計（獲得額 × 80%）
- 影響を受けるチームの**翌シーズンの人数**（例: 24 → 22 名）
- 下限22名を割るチームがある場合は**警告**

**反映した時点で補填金が入り、選手が実際に外れるのは翌シーズンから。**
今シーズンのスカッドも試合結果も変わらないので、シーズン途中でも安心して反映してよい。

| 状況 | 挙動 |
|---|---|
| オークションで獲得した選手 | 対象外にはなるが**補填金なし**（元々シーズン終了で離脱するため） |
| エントリーで取った選手（獲得額0） | 対象外にはなるが補填金なし |
| どのチームも保有していない選手 | 対象外になるだけ |
| 既に対象外の選手 | スキップされる。二重に補填されることはない |

誤って外した場合は一覧の「戻す」で復帰できる。ただし**補填金は取り消されない**。
打ち消したい場合は同額の罰金を計上する（何が起きたか予算履歴に残る）。

下限を割ったチームには、移籍市場での補充が必要になることを伝えておく。

### 補填の選択と精算

現実移籍・辞退・チーム変更で選手が使えなくなると、保有していたチームに
**補填の請求**が1件ずつ立つ。参加者は選手ごとに2択を選ぶ。

| 選択 | 内容 |
|---|---|
| 払い戻し | 獲得額 × 補填率（現実移籍80% / 辞退・チーム変更90%） |
| 入れ替え | **自分のクラブ**の選手で、誰も保有していない人と交換（獲得額0で記録） |

**手順**

1. 「補填の請求と精算」で**選択期限**を設定する。参加者はこの日時まで選べる
2. 参加者に「補填の選択」タブで選ぶよう伝える
3. 期限を過ぎたら **精算を実行**する。払い戻しの入金と入れ替えの受け渡しが一括で行われる

**期限内は精算できない。** まだ選べる人がいるうちに締めてしまう事故を防いでいる。

未選択のまま期限を過ぎた請求は Config の `claim_default_choice`（既定「払い戻し」）
として処理される。何も選ばなかった人が損をしないようにするため。

参加者が X でしか連絡してこない場合は、「補填の選択」タブでチームを切り替えれば
**主催者が代理で入力できる**（期限後でも可）。

精算は**新シーズンの移籍市場が開く前**に済ませる。予算が確定していないと、
参加者が使える額を把握できないまま交渉することになる。

### 辞退・チーム変更

参加者が抜ける、または使用クラブを変える場合は「辞退・チーム変更」で処理する。

**そのクラブの選手は全員が大会の対象外になる。** 選手プールは参加クラブの
選手の集合なので、クラブが抜ければその選手も抜ける。保有している他チームには
自動で90%の請求が立つ。

| 種別 | チーム | スカッド | 予算 |
|---|---|---|---|
| 辞退 | 非アクティブになる | 全員が離脱 | そのまま |
| チーム変更 | 名前が変わり「新規」扱いに戻る | 全員が離脱 | **初期値にリセット** |

**チーム変更は完全リセット。** 新規参加者とまったく同じ状態から始まる。

- スカッドは全員解散。**移籍で獲得した他クラブの選手も手放す**
- 予算は `new_team_initial_budget`（既定0）に戻る
- プロテクト・エントリー・進行中の移籍申請も無効になる

こうしないと、買い集めた戦力と予算を持ったままクラブを乗り換えられてしまい、
チーム変更が有利な選択になってしまう。

予算のリセットは行を消さず、**差額のマイナス取引を1行足して**行う。
予算履歴に「チーム変更リセット」として残るので、後から確認できる。

抜ける本人には請求は立たない。チーム変更した人は、新しいクラブでエントリーし直す。

**取り消せない。** 実行前に本人の意思を確認すること。

### シーズン終了処理

「運営・進行」タブの **シーズンを終了する**。実行前に引継ぎ先シーズンを作っておく。

1. **リーグ順位賞金** — GM1（二部制なら GM2 も）。1試合も消化していないチームは対象外
2. **GMリーグ杯** — 優勝・準優勝・ベスト4（準決勝敗退の2チームそれぞれに満額）
3. **GMスーパーカップ** — 優勝・準優勝。配信にチェックがあれば配信料も両チームへ
4. **得点王賞金** — GM1リーグ / GM2リーグ / GMリーグ杯 の3つを別々に集計
5. 残予算 × 10% を手数料として控除（**賞金・配信料をすべて計上した後**の残高が母数）
6. 期限切れ（オークション・全期期限付き）の選手を離脱
7. 引継ぎ先シーズンへスカッドをコピー。**現実移籍で対象外にした選手はここで外れる**
8. status を `終了` に

同順位・同点は**該当チーム全てに満額**支給する（按分しない）。
実行後は結果が画面に一覧表示されるので、支給漏れがないか目視で確認する。

**取り消せない。** 二重実行は自動で拒否されるが、間違った引継ぎ先を指定した場合は
シートを手で直すことになる。実行前に引継ぎ先の名前を確認ダイアログでよく見る。

---

## 3. 承認業務

| 対象 | 画面 | 承認前の状態 |
|---|---|---|
| エントリー | エントリー承認 | Rosters は `申請中`。スカッドに出ない |
| 移籍 | 移籍承認 | Rosters も BudgetTx も一切動かない |
| 試合 | 試合 → 一覧の各行 | 順位表・ランキングに反映されない |

**承認するまで何も確定しない**ので、迷ったら保留にしてよい。
差し戻せば申請者が直して再提出できる。

移籍だけは3段階（買い手申請 → 売り手同意 → 主催者承認）になっている。
売り手が同意していない移籍は主催者承認の一覧に出てこない。

---

## 4. 困ったときに

| 症状 | 原因と対処 |
|---|---|
| 参加者がログインできない | Users シートに未登録／OAuth のテストユーザー未追加 |
| `invalid_token` が出る | ログインから1時間経過。リロードして再ログイン |
| 「送信に失敗」と出たが反映されている | 通信エラーで結果だけ受け取れなかった状態。**二重に操作する前に画面を再読み込みして確認する** |
| プロテクトできない | 受付期間外。「プロテクト」タブで期間を確認 |
| 移籍が申請できない | 市場期間外／予算不足（承認待ちの分も引かれる）／人数が22〜35を外れる |
| 2レグ目が登録できない | `レグ` が未指定。1st / 2nd を選ぶ |
| 順位表が更新されない | 試合が未承認。「試合」タブで承認する |
| 参加希望者が「受け付けていません」と言う | 「参加登録承認」タブで受付がONか、合言葉が空でないかを確認 |
| 承認したのにログインできない | 本人が別の Google アカウントでログインしている可能性。申請時の email を確認 |
| X のリンクが出ない | そのユーザーが X ID 未設定。本人にダッシュボードから設定してもらう |
| 直したのに画面が変わらない | ブラウザのキャッシュ。**Ctrl+Shift+R** |

### データを直接直すとき

シートを手で編集してよいのは、承認フローに乗らない事故の修復だけ。
編集する場合は以下に注意する。

- **予算は BudgetTx に行を足して調整する。** 既存行の金額を書き換えると履歴が壊れる
- Rosters の `status` は `申請中` / `在籍` / `離脱` のいずれか
- `acquired_cost` は補填金の母数。移籍の実額と揃える

---

## 5. 定期的にやること

- **選手の `real_club` 更新** — 現実の移籍は主催者が手動で反映する。
  大会参加外クラブへ移籍した選手は `eligible` を `FALSE` にすると、
  翌シーズンのエントリーで弾かれる
- **Clubs シートの更新** — 昇降格やクラブ名変更があったら直接編集する。
  コードの修正は不要
- **スポンサー収益** — 各シーズンの第1次移籍市場の前に入力する
